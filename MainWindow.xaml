<Window x:Class="UncomClc.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" xmlns:local="clr-namespace:UncomClc"
        mc:Ignorable="d"
        Icon="pack://application:,,,/Images/uncomtech.png"
        Title="UncomClc" Height="1113" Width="2000"
        WindowState="Maximized">

    <Window.Resources>
        <local:WarningConverter x:Key="WarningConverter"/>
        <Style x:Key="CircleButtonStyle" TargetType="Button">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Grid>
                            <Ellipse Fill="{TemplateBinding Background}" 
                                 Stroke="{TemplateBinding BorderBrush}" 
                                 StrokeThickness="0"/>
                            <ContentPresenter HorizontalAlignment="Center" 
                                        VerticalAlignment="Center"/>
                        </Grid>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Background" Value="#FFDDDDDD"/>
            <Setter Property="BorderBrush" Value="#FF707070"/>
            <Setter Property="Foreground" Value="Black"/>
            <Setter Property="Width" Value="20"/>
            <Setter Property="Height" Value="20"/>
        </Style>
        <Style x:Key="TemperatureTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                            Focusable="False"
                                            HorizontalScrollBarVisibility="Hidden"
                                            VerticalScrollBarVisibility="Hidden"  
                                            />

                                    <!-- Подсказка "°C" -->
                                    <TextBlock x:Name="DegreeSymbol"
                                          Text="°C"
                                          Opacity="0.3"
                                          IsHitTestVisible="False"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Right"
                                          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="VoltageTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                    Focusable="False"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden"  
                                    />

                                    <!-- Подсказка "°C" -->
                                    <TextBlock x:Name="DegreeSymbol"
                                  Text="В"
                                  Opacity="0.3"
                                  IsHitTestVisible="False"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Right"
                                  Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="CurrnetTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                        Focusable="False"
                                        HorizontalScrollBarVisibility="Hidden"
                                        VerticalScrollBarVisibility="Hidden"/>

                                    <!-- Подсказка "°C" -->
                                    <TextBlock x:Name="DegreeSymbol"
                                    Text="A"
                                    Opacity="0.3"
                                    IsHitTestVisible="False"
                                    VerticalAlignment="Center"
                                    HorizontalAlignment="Right"
                                    Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="MetersTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                            Focusable="False"
                                            HorizontalScrollBarVisibility="Hidden"
                                            VerticalScrollBarVisibility="Hidden"  
                                            />

                                    <!-- Подсказка "°C" -->
                                    <TextBlock x:Name="DegreeSymbol"
                                          Text="м"
                                          Opacity="0.3"
                                          IsHitTestVisible="False"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Right"
                                          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="MMTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                                Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                            Focusable="False"
                                            HorizontalScrollBarVisibility="Hidden"
                                            VerticalScrollBarVisibility="Hidden"  
                                            />

                                    <!-- Подсказка "°C" -->
                                    <TextBlock x:Name="DegreeSymbol"
                                          Text="мм"
                                          Opacity="0.3"
                                          IsHitTestVisible="False"
                                          VerticalAlignment="Center"
                                          HorizontalAlignment="Right"
                                          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="CountTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                    Focusable="False"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden"  
                                    />

                                    <TextBlock x:Name="DegreeSymbol"
                                  Text="шт"
                                  Opacity="0.3"
                                  IsHitTestVisible="False"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Right"
                                  Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="LenghtCountTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                Background="{TemplateBinding Background}"
                BorderBrush="{TemplateBinding BorderBrush}"
                BorderThickness="{TemplateBinding BorderThickness}"
                SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                            Focusable="False"
                            HorizontalScrollBarVisibility="Hidden"
                            VerticalScrollBarVisibility="Hidden"  
                            />

                                    <TextBlock x:Name="DegreeSymbol"
                          Text="м/шт"
                          Opacity="0.3"
                          IsHitTestVisible="False"
                          VerticalAlignment="Center"
                          HorizontalAlignment="Right"
                          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="VatCountTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
        Background="{TemplateBinding Background}"
        BorderBrush="{TemplateBinding BorderBrush}"
        BorderThickness="{TemplateBinding BorderThickness}"
        SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                    Focusable="False"
                    HorizontalScrollBarVisibility="Hidden"
                    VerticalScrollBarVisibility="Hidden"  
                    />

                                    <TextBlock x:Name="DegreeSymbol"
                  Text="Вт/м"
                  Opacity="0.3"
                  IsHitTestVisible="False"
                  VerticalAlignment="Center"
                  HorizontalAlignment="Right"
                  Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="KVatCountTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
            Focusable="False"
            HorizontalScrollBarVisibility="Hidden"
            VerticalScrollBarVisibility="Hidden"  
            />

                                    <TextBlock x:Name="DegreeSymbol"
          Text="кВт"
          Opacity="0.3"
          IsHitTestVisible="False"
          VerticalAlignment="Center"
          HorizontalAlignment="Right"
          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="OmCountTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
            Focusable="False"
            HorizontalScrollBarVisibility="Hidden"
            VerticalScrollBarVisibility="Hidden"  
            />

                                    <TextBlock x:Name="DegreeSymbol"
          Text="Ом/км"
          Opacity="0.3"
          IsHitTestVisible="False"
          VerticalAlignment="Center"
          HorizontalAlignment="Right"
          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="MM2CountTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
Background="{TemplateBinding Background}"
BorderBrush="{TemplateBinding BorderBrush}"
BorderThickness="{TemplateBinding BorderThickness}"
SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
            Focusable="False"
            HorizontalScrollBarVisibility="Hidden"
            VerticalScrollBarVisibility="Hidden"  
            />

                                    <TextBlock x:Name="DegreeSymbol"
          Text="мм2"
          Opacity="0.3"
          IsHitTestVisible="False"
          VerticalAlignment="Center"
          HorizontalAlignment="Right"
          Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
        <Style x:Key="ErrorTextBoxStyle" BasedOn="{StaticResource TemperatureTextBoxStyle}" TargetType="TextBox">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ResultView.IsShellTemp}" Value="True">
                    <Setter Property="Background" Value="#FFFFE0E0"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для панели с ошибкой -->
        <!--<Style x:Key="ErrorStackPanelStyle2" TargetType="StackPanel">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ResultView.IsLengthError}" Value="True">
                    <Setter Property="Background" Value="Red"/>
                    <Setter Property="Margin" Value="3"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>-->
        <Style x:Key="ErrorTextBoxStyle2" BasedOn="{StaticResource CurrnetTextBoxStyle}" TargetType="TextBox">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ResultView.IsStartCurrent}" Value="True">
                    <Setter Property="Background" Value="#FFFFE0E0"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>

        <!-- Стиль для панели с ошибкой --><!--
        <Style x:Key="ErrorStackPanelStyle" TargetType="StackPanel">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ResultView.IsStartCurrent}" Value="True">
                    <Setter Property="Background" Value="Red"/>
                    <Setter Property="Margin" Value="3"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>-->
        <Style x:Key="ErrorTextBoxStyle3" BasedOn="{StaticResource MetersTextBoxStyle}" TargetType="TextBox">
            <Style.Triggers>
                <DataTrigger Binding="{Binding ResultView.IsLenght}" Value="True">
                    <Setter Property="Background" Value="#FFFFE0E0"/>
                    <Setter Property="BorderBrush" Value="Red"/>
                    <Setter Property="BorderThickness" Value="2"/>
                </DataTrigger>
            </Style.Triggers>
        </Style>
    </Window.Resources>
    

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="40" />
            <RowDefinition Height="0.9*" />
            <!--<RowDefinition Height="0.2*" />-->
            <RowDefinition Height="0.7*" />
        </Grid.RowDefinitions>

        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="57*" />
            <ColumnDefinition Width="172*"/>
            <!-- Первый вертикальный раздел -->
            <ColumnDefinition Width="471*" />
            <!-- Второй вертикальный раздел -->
        </Grid.ColumnDefinitions>

        <!-- Верхний горизонтальный раздел -->
        <StackPanel Grid.Row="0" Grid.ColumnSpan="3" Grid.RowSpan="1">

            <ToolBar Background="Transparent" >

                <Menu Background="White">
                    <MenuItem Width="32" Height="25" Margin="5" Background="Tan" BorderBrush="Black">
                        
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/folder.png" Width="20" Height="20"/>
                        </MenuItem.Icon>
                        
                        <MenuItem Header="Создать" Command="{Binding CreateCommand}">
                            <MenuItem.Icon>
                                <Image Source="pack://application:,,,/Images/CreateFile.png" Width="20" Height="20"/>
                            </MenuItem.Icon>
                        </MenuItem>

                        <MenuItem Header="Открыть" Command="{Binding OpenCommand}">
                            <MenuItem.Icon>
                                <Image Source="pack://application:,,,/Images/OpenFile.png" Width="20" Height="20"/>
                            </MenuItem.Icon>
                        </MenuItem>

                        <MenuItem Header="Сохранить" Command="{Binding SaveCommand}">
                            <MenuItem.Icon>
                                <Image Source="pack://application:,,,/Images/SaveFile.png" Width="20" Height="20"/>
                            </MenuItem.Icon>
                        </MenuItem>

                    </MenuItem >
                    <TextBox x:Name="FileName" Width="150" Height="25" FontSize="16" FontWeight="Bold" Text="{Binding CurrentFile, UpdateSourceTrigger=PropertyChanged}" IsEnabled="False"/>
                    <MenuItem Width="145" Height="25" Margin="5" Background="Tan" BorderBrush="Black" Command="{Binding AddPipeCommand}">
                        <MenuItem.Header>
                            <TextBlock Text="Добавить трубопровод" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </MenuItem.Header>
                    </MenuItem>
                    <MenuItem  Width="100" Height="25" Margin="5" Background="Tan" BorderBrush="Black" Command="{Binding Calculate}">
                        <MenuItem.Header>
                            <TextBlock Text="Рассчитать" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </MenuItem.Header>
                    </MenuItem>
                </Menu>
                <Menu Background="White">
                    <MenuItem Header="" Command="{Binding InfoCommand}" Height="35" Width="45">
                        <MenuItem.Icon>
                            <Image Source="pack://application:,,,/Images/Info.png" Width="30" Height="30"/>
                        </MenuItem.Icon>
                    </MenuItem>
                </Menu>
            </ToolBar>
            
        </StackPanel>

        <!-- Верхняя 1 часть с вертикальными разделами -->
        <Border Grid.Row="1" Grid.Column="0" Background="Gray" Margin="5" Grid.ColumnSpan="2">
            <TreeView x:Name="PipeLinesTree" FontSize="22" ItemsSource="{Binding PipeLines}" SelectedItemChanged="PipeLinesTree_SelectedItemChanged"
                      MouseDoubleClick="PipeLinesTree_MouseDoubleClick"           
                      PreviewMouseLeftButtonDown="PipeLinesTree_PreviewMouseLeftButtonDown"
                      PreviewMouseMove="PipeLinesTree_PreviewMouseMove"
                      DragOver="PipeLinesTree_DragOver"
                      Drop="PipeLinesTree_Drop"
                      AllowDrop="True">
                <TreeView.ItemTemplate>
                    <HierarchicalDataTemplate>
                        <TextBlock Text="{Binding Name}" 
                     Foreground="{Binding HasWarning, Converter={StaticResource WarningConverter}}"/>
                    </HierarchicalDataTemplate>
                </TreeView.ItemTemplate>
                <TreeView.ContextMenu>
                    <ContextMenu>
                        <MenuItem Header="Удалить" Click="Delete_Click" Command="{Binding DeleteCommand}"> 
                            <MenuItem.Icon>
                                <Image Source="pack://application:,,,/Images/delete.png" Width="20" Height="20"/>
                            </MenuItem.Icon>
                        </MenuItem>
                        <MenuItem Header="Копировать" Command="{Binding CopyCommand}">
                            <MenuItem.Icon>
                                <Image Source="pack://application:,,,/Images/Copy.png" Width="20" Height="20"/>
                            </MenuItem.Icon>
                        </MenuItem>
                    </ContextMenu>
                </TreeView.ContextMenu>
            </TreeView>
        </Border>

        <!-- Верхняя 2 часть с вертикальными разделами -->
        <TabControl x:Name="MyTabControl" Grid.Row="1" Grid.Column="2" Margin="5" Background="Tan">
            <TabItem Header="Исходные данные для расчета нагревательных секций из кабеля нагревостойкого с минеральной изоляцией" Grid.Row="1" Grid.Column="2" >
                <ScrollViewer HorizontalScrollBarVisibility="Auto">

                    <StackPanel Orientation="Horizontal" Margin="10" >

                        <StackPanel Orientation="Vertical">

                            <GroupBox Header="Труба" Margin="5">
                                <StackPanel Orientation="Vertical" Margin="15">
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Тип:" />
                                        <ComboBox Width="160"  x:Name="Pipes" Height="25" SelectedItem="{Binding ProcessVM.Pipe}"/>
                                        <Button x:Name="EditPipes" Content="..." Width="60" Height="25" Margin="10,0,0,0" Click="EditPipes_Click"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Наружный диаметр:"/>
                                        <TextBox x:Name="Diam" Width="60" Height="25" Text="{Binding ProcessVM.Diam, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MMTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label x:Name="Thickness" Content="Толщина стенки:"/>
                                        <TextBox Width="60" Height="25" Text="{Binding ProcessVM.Thickness, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MMTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label x:Name="Lenght" Content="Длина:"/>
                                        <TextBox Width="60" Height="25" Text="{Binding ProcessVM.Lenght, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        <Label Content="Коэф. запаса:"/>
                                        <TextBox x:Name="PipeKoef" Width="60"  Height="25" Padding="0,3,0,0" Text="{Binding ProcessVM.PipeKoef, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                                        <Label Content="Задвижки: "/>
                                        <TextBox x:Name="ValvesCount" Margin="0,0,5,0" Width="60" Height="25" Text="{Binding ProcessVM.ValveCount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CountTextBoxStyle}"/>
                                        <TextBox x:Name="ValvesLengt"  Width="70" Height="25" Text="{Binding ProcessVM.ValveLenght, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource LenghtCountTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                                        <Label Content="Фланцы: "/>
                                        <TextBox x:Name="FlangsCount" Margin="0,0,5,0" Width="60" Height="25" Text="{Binding ProcessVM.FlangCount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CountTextBoxStyle}"/>
                                        <TextBox x:Name="FlangsLengt"  Width="70" Height="25" Text="{Binding ProcessVM.FlangLength, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource LenghtCountTextBoxStyle}" />
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
                                        <Label Content="Опоры: "/>
                                        <TextBox x:Name="SupportsCount" Margin="0,0,5,0" Width="60" Height="25" Text="{Binding ProcessVM.SupportCount, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CountTextBoxStyle}"/>
                                        <TextBox x:Name="SupportsLenght"  Width="70" Height="25" Text="{Binding ProcessVM.SupportLenght, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource LenghtCountTextBoxStyle}" />
                                    </StackPanel>

                                </StackPanel>

                            </GroupBox>

                            <GroupBox Header="Теплоизоляция 1 слой" Margin="5" >

                                <StackPanel Orientation="Vertical" Margin="15">

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Тип:"/>
                                        <ComboBox Width="160" Margin="10,0" Height="25" x:Name="ThermalIsolationCombobox" SelectedItem="{Binding ProcessVM.ThermalIsolation}"/>
                                        <Button x:Name="ThermalIsolationEditButton" Content="..." Height="25" Width="60" Click="ThermalIsolationEditButton_Click"  HorizontalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Толщина:"/>
                                        <TextBox Height="25" Width="60" x:Name="ThicknessIsolation" Text="{Binding ProcessVM.IsolationThickness, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MMTextBoxStyle}"/>
                                    </StackPanel>

                                </StackPanel>
                            </GroupBox>

                            <GroupBox Header="Теплоизоляция 2 слой" Margin="5">

                                <StackPanel Orientation="Vertical" Margin="15">

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Тип:"/>
                                        <ComboBox Width="160" Margin="10,0" Height="25" x:Name="ThermalIsolationCombobox2" SelectedItem="{Binding ProcessVM.ThermalIsolation2}">
                                        </ComboBox>
                                        <Button x:Name="ThermalIsolationEditButton2" Content="..." Height="25" Width="60" Click="ThermalIsolationEditButton_Click" HorizontalAlignment="Center"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Толщина:"/>
                                        <TextBox Height="25" Width="60" x:Name="ThicknessIsolation2" Text="{Binding ProcessVM.IsolationThickness2, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ProcessVM.IsIsolationThicknessEnabled}"  Style="{StaticResource MMTextBoxStyle}"/>
                                    </StackPanel>

                                </StackPanel>
                            </GroupBox>

                        </StackPanel>

                        <StackPanel Orientation="Vertical">

                            <GroupBox Header="Окружающая среда" Margin="5">
                                <StackPanel Orientation="Vertical" Margin="15">

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Мин. температура окр. среды:"/>
                                        <TextBox x:Name="MinEnvironmentTemp" Width="60" Height="25" Text="{Binding EnvironmentVM.MinEnvironmentTemp, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TemperatureTextBoxStyle}">
                                        </TextBox>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Макс. температура окр. среды:"/>
                                        <TextBox x:Name="MaxEnvironmentTemp" Width="60" Height="25" Text="{Binding EnvironmentVM.MaxEnvironmentTemp, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TemperatureTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Размещение трубопровода:"/>
                                        <ComboBox x:Name="PipelinePlacement" Width="130" Height="25" ItemsSource="{Binding EnvironmentVM.PipelinePlacementOptions}" SelectedItem="{Binding EnvironmentVM.PipelinePlacement}"/>
                                    </StackPanel>

                                </StackPanel>
                            </GroupBox>

                            <GroupBox Header="Технологический процесс" Margin="5">

                                <StackPanel Orientation="Vertical" Margin="15">

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label x:Name="SupportedTemp" Content="Поддерживаемая температура:"/>
                                        <TextBox Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.SupportedTemp, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TemperatureTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label x:Name="MaxTechProductTemp" Content="Макс. техн. температура продукта:"/>
                                        <TextBox Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.MaxTechProductTemp, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TemperatureTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label x:Name="MaxAddProductTemp"  Content="Макс. доп. температура продукта:"/>
                                        <TextBox Width="60" Height="25" Text="{Binding ProcessVM.MaxAddProductTemp, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TemperatureTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Пропарка:"/>
                                        <ComboBox x:Name="StreamingStatus" Width="50" Height="25" ItemsSource="{Binding ProcessVM.SteamingOptions}" SelectedItem="{Binding ProcessVM.SteamingStatus}"/>
                                        <Label Content="Темп. пропарки:"/>
                                        <TextBox x:Name="StreamingTemperature" Width="60" Height="25" Text="{Binding ProcessVM.StreamingTemperature, UpdateSourceTrigger=PropertyChanged}" IsEnabled="{Binding ProcessVM.IsSteamingTemperatureEnabled}" Style="{StaticResource TemperatureTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Темп. класс ВЗЭО:"/>
                                        <ComboBox x:Name="TemperatureClass" Width="50" Height="25" Margin="0,0,10,0" ItemsSource="{Binding ProcessVM.TemperatureClassOptions}" SelectedItem="{Binding ProcessVM.TemperatureClass}"/>
                                        <TextBox x:Name="TemperatureClassValue" Width="60" Height="25" Text="{Binding ProcessVM.TemperatureClassValue}" IsEnabled="{Binding ProcessVM.IsTemperatureClassEnabled}" Style="{StaticResource TemperatureTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Рабочая среда:"/>
                                        <ComboBox x:Name="WorkEnvironment" Width="160" Height="25" ItemsSource="{Binding PowerSupplyParametersVM.WorkEnvironmentOptions}" SelectedItem="{Binding PowerSupplyParametersVM.WorkEnvironment, UpdateSourceTrigger=PropertyChanged, Delay=50, NotifyOnSourceUpdated=True}"/>
                                    </StackPanel>


                                </StackPanel>
                            </GroupBox>

                        </StackPanel>

                        <StackPanel Orientation="Vertical">

                            <GroupBox Header="Параметры питающей сети" Margin="5">
                                <StackPanel Orientation="Vertical" Margin="15">

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Напряжение фазное:"/>
                                        <TextBox x:Name="PhaseVoltage" Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.PhaseVoltage, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource VoltageTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Напряжение линейное:"/>
                                        <TextBox x:Name="LineVoltage" Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.LineVoltage, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource VoltageTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Номинальный ток АВ:"/>
                                        <TextBox x:Name="Currrent" Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.Current, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CurrnetTextBoxStyle}"/>
                                    </StackPanel>

                                </StackPanel>
                            </GroupBox>

                            <GroupBox Header="Параметры КСЭО" Margin="5">
                                <StackPanel Orientation="Vertical" Margin="15">

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Питание:"/>
                                        <ComboBox x:Name="Nutrition" Width="100" Height="25" Margin="0,0,10,0" ItemsSource="{Binding PowerSupplyParametersVM.NutritionOptions}" SelectedItem="{Binding PowerSupplyParametersVM.Nutrition}"/>
                                        <TextBox x:Name="WorkLoad"  Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.WorkLoad, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource VoltageTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Схема соединения:"/>
                                        <ComboBox x:Name="ConnectionScheme" Width="90" Height="25" ItemsSource="{Binding PowerSupplyParametersVM.ConnectionSchemeOptions}" SelectedItem="{Binding PowerSupplyParametersVM.ConnectionScheme}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Мин. темп. включения:"/>
                                        <TextBox x:Name="MinTempOn" Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.MinTempOn, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource TemperatureTextBoxStyle}"/>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Тип кабеля:"/>
                                        <ComboBox x:Name="CableType" Width="90" Height="25" ItemsSource="{Binding PowerSupplyParametersVM.CableTypeOptions}" SelectedItem="{Binding PowerSupplyParametersVM.CableType}"/>
                                        <Button Width="25" Height="25" Margin="5,0,0,0" 
                                            Style="{StaticResource CircleButtonStyle}" Click="InfoButton">
                                            <Image Source="/Images/info.png" Width="25" Height="25"/>
                                        </Button>
                                    </StackPanel>

                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Кол-во жил:"/>
                                        <TextBox x:Name="NumberCores" Width="20" Height="25" Padding="3,3,0,0" Text="{Binding PowerSupplyParametersVM.NumberCores, UpdateSourceTrigger=PropertyChanged}" IsEnabled="False"/>
                                    </StackPanel>
                                    <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                                        <Label Content="Длина устан. части секции:"/>
                                        <TextBox x:Name="LenghtSection" Width="60" Height="25" Text="{Binding PowerSupplyParametersVM.LenghtSection, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MetersTextBoxStyle}"/>
                                    </StackPanel>

                                </StackPanel>
                            </GroupBox>

                        </StackPanel>

                    </StackPanel>
                </ScrollViewer>
            </TabItem>
            <TabItem Header="Дополнительные функции" IsEnabled="False" Visibility="Hidden">

            </TabItem>
        </TabControl>

        <!--<Border x:Name="Message" Grid.Row="2" Grid.ColumnSpan="3" Margin="5" Background="LightGray">
            <ScrollViewer>
                <TextBlock x:Name="MessagesBlock">

                </TextBlock>
            </ScrollViewer>
        </Border>-->

        <!-- Нижняя часть с горизонтальным разделом -->
        <Border Grid.Row="3" Grid.ColumnSpan="3" Background="White">
            <ScrollViewer>
                <Grid>
                    <StackPanel Orientation="Vertical">
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <Label Content="Результаты расчета" FontSize="18" FontWeight="Bold" />
                        </StackPanel>
                        <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                            <StackPanel Orientation="Vertical" Margin="10">

                                <GroupBox>
                                    <GroupBox.Header>
                                        <TextBlock Text="Трубопровод" FontSize="16" FontWeight="Bold"/>
                                    </GroupBox.Header>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Расчетные тепловые потери с запасом: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.CalculatedHeatLoss, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource VatCountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Длина одной нитки кабеля с запасом: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Lobsh, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Запас длины на трубу: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Lzap, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Запас длины на задвижки: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Lzadv, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5"  HorizontalAlignment="Right">
                                            <Label Content="Запас длины на фланцы: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Lfl, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Запас длины на опоры: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Lop, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </GroupBox>
                                
                                
                                
                            </StackPanel>
                            <StackPanel Orientation="Vertical" Margin="10">

                                <GroupBox>
                                    <GroupBox.Header>
                                        <TextBlock Text="Параметры КСЭО" FontSize="16" FontWeight="Bold"/>
                                    </GroupBox.Header>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Мощность обогрева трубы рабочая: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Pobogr, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource VatCountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Мощность одной нитки кабеля рабочая: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Pkabrab, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource VatCountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Схема соединения: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Scheme, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Кол-во нагревательных секций: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Ssec, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource CountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Длина нагревательного кабеля секции: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.HeatCableLength, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource ErrorTextBoxStyle3}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Максимальная температура оболочки кабеля: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Tobol, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource ErrorTextBoxStyle}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </GroupBox>

                            </StackPanel>
                            <StackPanel Orientation="Vertical" Margin="10">

                                <GroupBox>
                                    <GroupBox.Header>
                                        <TextBlock Text="Условное обозначение нагревательной секции" FontSize="16" FontWeight="Bold"/>
                                    </GroupBox.Header>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <TextBox Width="550" Height="25" Text="{Binding ResultView.CH, UpdateSourceTrigger=PropertyChanged}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Марка кабеля: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Mark, UpdateSourceTrigger=PropertyChanged}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Сечение жилы: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Cross, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource MM2CountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Номинальное эквив. сопр. жил при 20°C: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Resistance, UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource OmCountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Рабочее напряжение: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Urab, UpdateSourceTrigger=PropertyChanged}"  Style="{StaticResource VoltageTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Расчетная мощность при 20°C: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Psec20, UpdateSourceTrigger=PropertyChanged, StringFormat=N2}" Style="{StaticResource KVatCountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Длина нагревательного кабеля секции: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Lsec, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Длина холодного ввода: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Lust, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource MetersTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Температурный класс ВЗЭО: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.TempClass, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </GroupBox>

                            </StackPanel>
                            <StackPanel Orientation="Vertical" Margin="10">

                                <GroupBox>
                                    <GroupBox.Header>
                                        <TextBlock Text="Электрические характеристики нагрев. секции" FontSize="16" FontWeight="Bold"/>
                                    </GroupBox.Header>
                                    <StackPanel Orientation="Vertical">
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Питание "/>
                                            <TextBox Width="100" Text="{Binding ResultView.Pit, UpdateSourceTrigger=PropertyChanged}" Height="25"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Рабочее напряжение: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Urab, UpdateSourceTrigger=PropertyChanged}" Style="{StaticResource VoltageTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Стартовый ток секции: "/>
                                            <TextBox Width="100" Text="{Binding ResultView.Ivklmin, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Height="25" Style="{StaticResource ErrorTextBoxStyle2}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Рабочий ток секции: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Irab, UpdateSourceTrigger=PropertyChanged, StringFormat=N1}" Style="{StaticResource CurrnetTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Стартовая мощность секции: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Psecvklmin, UpdateSourceTrigger=PropertyChanged, StringFormat=N3}" Style="{StaticResource KVatCountTextBoxStyle}"/>
                                        </StackPanel>
                                        <StackPanel Orientation="Horizontal" Margin="5" HorizontalAlignment="Right">
                                            <Label Content="Рабочая мощность секции: "/>
                                            <TextBox Width="100" Height="25" Text="{Binding ResultView.Psecrab, UpdateSourceTrigger=PropertyChanged, StringFormat=N3}" Style="{StaticResource KVatCountTextBoxStyle}"/>
                                        </StackPanel>
                                    </StackPanel>
                                </GroupBox>

                            </StackPanel>
                        </StackPanel>
                    </StackPanel>
                </Grid>
            </ScrollViewer>
        </Border>
    </Grid>
</Window>
