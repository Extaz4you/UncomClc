<Window x:Class="UncomClc.Views.Valves.ValvesSupportsFlangs"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:UncomClc.Views.Valves"
        mc:Ignorable="d"
        ResizeMode="NoResize"
        WindowStyle="SingleBorderWindow"
        Title="Задвижки и опоры" Height="550" Width="400">
    <Window.Resources>
        <Style x:Key="CountTextBoxStyle" TargetType="TextBox">
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="TextBox">
                        <Grid>
                            <!-- Базовый Border из стандартного шаблона -->
                            <Border x:Name="border" 
                        Background="{TemplateBinding Background}"
                        BorderBrush="{TemplateBinding BorderBrush}"
                        BorderThickness="{TemplateBinding BorderThickness}"
                        SnapsToDevicePixels="True">

                                <!-- Основной контент -->
                                <Grid>
                                    <ScrollViewer x:Name="PART_ContentHost"
                                    Focusable="False"
                                    HorizontalScrollBarVisibility="Hidden"
                                    VerticalScrollBarVisibility="Hidden"  
                                    />

                                    <TextBlock x:Name="DegreeSymbol"
                                  Text="шт"
                                  Opacity="0.3"
                                  IsHitTestVisible="False"
                                  VerticalAlignment="Center"
                                  HorizontalAlignment="Right"
                                  Margin="0,0,5,0"/>
                                </Grid>
                            </Border>
                        </Grid>

                        <ControlTemplate.Triggers>
                            <!-- Стандартные триггеры для TextBox -->
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF7EB4EA"/>
                            </Trigger>
                            <Trigger Property="IsKeyboardFocused" Value="True">
                                <Setter TargetName="border" Property="BorderBrush" Value="#FF569DE5"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Foreground" Value="Gray"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Setter Property="Padding" Value="2,0,25,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Background" Value="#FFFFFFFF"/>
            <Setter Property="BorderBrush" Value="#FFABADB3"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Foreground" Value="#FF000000"/>
        </Style>
    </Window.Resources>
    <Grid>
        <ScrollViewer>
            <StackPanel Orientation="Vertical" Margin="5">
                <StackPanel Orientation="Vertical">

                    <GroupBox Header="Задвижки" Margin="5">
                        <StackPanel Orientation="Vertical">

                            <StackPanel Orientation="Horizontal" Margin="5">
                                <Label Content="Тип 1:" Margin="5"/>
                                <ComboBox Width="150" Margin="5"/>
                                <TextBox Width="50" Margin="5" Style="{StaticResource CountTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="5">
                                <Label Content="Тип 1:" Margin="5"/>
                                <ComboBox Width="150" Margin="5"/>
                                <TextBox Width="50" Margin="5" Style="{StaticResource CountTextBoxStyle}"/>
                            </StackPanel>
                            
                            <StackPanel Orientation="Horizontal" Margin="5">
                                <Label Content="Тип 1:" Margin="5"/>
                                <ComboBox Width="150" Margin="5"/>
                                <TextBox Width="50" Margin="5" Style="{StaticResource CountTextBoxStyle}"/>
                            </StackPanel>

                        </StackPanel>
                    </GroupBox>


                    <GroupBox Header="Опоры" Margin="5">
                        <StackPanel Orientation="Vertical">

                            <StackPanel Orientation="Horizontal" Margin="5">
                                <Label Content="Тип 1:" Margin="5"/>
                                <ComboBox Width="150" Margin="5"/>
                                <TextBox Width="50" Margin="5" Style="{StaticResource CountTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="5">
                                <Label Content="Тип 1:" Margin="5"/>
                                <ComboBox Width="150" Margin="5"/>
                                <TextBox Width="50" Margin="5" Style="{StaticResource CountTextBoxStyle}"/>
                            </StackPanel>

                            <StackPanel Orientation="Horizontal" Margin="5">
                                <Label Content="Тип 1:" Margin="5"/>
                                <ComboBox Width="150" Margin="5"/>
                                <TextBox Width="50" Margin="5" Style="{StaticResource CountTextBoxStyle}"/>
                            </StackPanel>

                        </StackPanel>
                    </GroupBox >

                    <GroupBox Header="Фланцы" Margin="5">
                        <StackPanel Orientation="Horizontal" Margin="5">
                            <Label Content="Тип 1:" Margin="5"/>
                            <ComboBox Width="150" Margin="5"/>
                            <TextBox Width="50" Margin="5" Style="{StaticResource CountTextBoxStyle}"/>
                        </StackPanel>
                    </GroupBox>

                </StackPanel>

                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <StackPanel Orientation="Horizontal" Margin="10">
                        <Button Content="ОК" Width="100" Margin="10" Height="25"  Click="Ok_Click"/>
                        <Button Content="Отмена" Width="100" Margin="10" Height="25" Click="Cancel_Click"/>
                    </StackPanel>
                </StackPanel>

            </StackPanel>
        </ScrollViewer>
    </Grid>
</Window>
